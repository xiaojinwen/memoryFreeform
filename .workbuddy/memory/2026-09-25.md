# 2026-09-25

## 仓库迁移：清空 git 历史 → 迁到 memory-freeform 远端
- 操作：`rm -rf .git` → `git init -b master` → `git add -A` → 单条初始提交 → `git remote add origin git@gitee.com:xiaojinwen/memory-freeform.git` → `git push -u origin master`。
- 旧历史（205 个提交到 fix130，origin 原为 `xiaojinwen/single-hand-mode`）**全部作废**。
- 新仓库只有 2 个提交：
  - `463cdec chore: 清空历史重建仓库并迁移至 memory-freeform 远端`（43 个跟踪文件，含当时 3 个未提交改动：build.gradle.kts、CornerWindowService.kt、FloatingBallService.kt）
  - `2f31d42 fix131: 记忆更新`（用户手动提交，改 MEMORY.md）
- 当前分支 master，已跟踪 origin/master，工作区干净。以后提交照旧 `git add <文件> && git commit -m "fixNN: 中文摘要"` + `git push`（默认推 master）。
- ⚠ 旧 .git 完整备份留在 **`E:\project\single-hand-mode-git-backup-20260925`**（110MB），确认新仓库没问题后可自行删除；注意它**不在**仓库内，别误当源码。
- ⚠ 备份目录在仓库内时会被 `git add -A` 卷入（110MB），所以先移到仓库外再 init。
- ⚠ Git Bash 里 `timeout` 会被 Windows 的 `TIMEOUT.EXE` 抢走，别用它做超时控制。
- 小意外：迁移期间用户在另一个终端提交了 `fix131: 记忆更新`，导致补推了一次（`463cdec..2f31d42`），属正常，非冲突。

## 追加：同时托管到 GitHub，主分支改名为 main
- 新增远端 `github = git@github.com:xiaojinwen/memoryFreeform.git`。
- `git branch -M main`（本地 master 已不存在）+ `git push -u origin main` + `git push -u github main`
  ⇒ gitee 与 github 上 **main** 分支都已建立，两边 HEAD 同为 `2f31d42`，无分叉。
- ⚠ **GitHub 首次推不上去**：`Host key verification failed`（`~/.ssh/known_hosts` 只有 gitee 的条目）。
  修法：`ssh-keyscan -H github.com 2>/dev/null >> ~/.ssh/known_hosts` 后即通。以后换机器/换远端先做这步。
- ⚠ `git remote add github ...` 报 "already exists" ⇒ 说明用户在另一终端同时加了，别重复 add，直接 `git ls-remote github` 验连通即可。
- ⚠ 分支改名后本地 `branch.main.merge` 会跟着变成 refs/heads/main（`-vv` 一度还显示 `[origin/master]`，推完即正）。
- 待用户按需处理：gitee/github 网页端把 **main 设为默认分支**（目前 gitee 默认仍是 master，
  那是我第一次 push 时建的）。代码提交照旧 `git add <文件>` → `git commit` → `git push`（两个远端都会收）。
- 遗留未提交（未推送，非问题）：`MEMORY.md` 改动、新增 `.workbuddy/memory/2026-09-25.md`。

## 追加：改回 master 主分支（同日晚，用户要求）
- `git branch -m main master`（用户在另一终端已先做，`git branch -m main master` 报 "no branch named 'main'"，无妨）。
- 推 `master` 到两端：`git push origin master`（gitee 早已同 commit，up-to-date）+ `git push github master`（新建）。
  上游重绑 `git branch --set-upstream-to=origin/master master`。
- gitee 的 `main` 已删（`git push origin --delete main`）；**github 的 `main` 删不掉** —— 报错
  `refusing to delete the current branch: refs/heads/main`，因为 GitHub 端默认分支是 main。
  ⇒ 用户须去 github.com/xiaojinwen/memoryFreeform/settings/branches 把 default 改成 master，main 才删得掉；
  或者干脆保留 main（内容与 master 同 commit 2f31d42，不影响 `git push`）。
- `origin/HEAD -> origin/master`，gitee 上默认分支已是 master；本地 `git fetch --prune` 清掉已删的远端引用。
- ⚠ 本机没装 `gh` CLI，GitHub 仓库设置只能网页端改。
- 现状：本地 master ↔ origin/master ↔ github/master 三者同为 `2f31d42`。

## 追加：双远端分支映射（用户要求 github→main、gitee→master）
- 做法：本地只留一个 `master` 分支，靠 push refspec 分流：
  `git config remote.github.push "refs/heads/master:refs/heads/main"`（origin 用默认同名行为）。
  于是 `git push origin` → gitee **master**，`git push github` → github **main**。无需两套本地分支。
- 验证方式：必须有个真提交才看得出来（commit 相同时 dry-run 都报 up-to-date）。
  借提交待处理的记忆文件做真推：`2f31d42..f3baa08 master -> master`（gitee）、
  `2f31d42..f3baa08 master -> main`（github），映射确认无误。
- 顺带把 `MEMORY.md` + `2026-09-25.md` 提交入库（`f3baa08 docs: 补充仓库迁移与双远端分支映射说明`），
  工作区已干净。
- ⚠ GitHub 的 `main` 仍是默认分支（所以删不掉），但现在它就是 github 的接收分支，留着正好。

## fix132：悬浮球「最近任务/应用」面板高度封顶（单手操作）
- 问题：面板高度跟小窗设置尺寸走，1080×2400 上列表顶到屏幕上 ~70%，拇指够不着。
  旧 fix131 只给「最近任务」减 50dp、「应用」不减，缓解不够。
- 改法：`CornerWindowService.openPanel()` 里把面板高度按**屏幕可用高度 50%** 封顶
  （`maxH = (sh*0.5f).toInt()`，复用原 `sh`/`bottom`，单位一致），两种面板统一收；下限 200dp。
  列表本身可滚动，矮一点更好点。「离底边间距」锚定不变，面板仍是贴底那块。
- 版本：versionCode 131→132、versionName 1.0.131→1.0.132。`f358b30 fix132: ...` 已推
  gitee master + github main。
- 交付 APK：`E:\搬家文件夹\singlehand-fix132.apk`（纯 App 侧改动，force-stop 即生效，无需重启手机）。
- 可调：觉得还不够矮/太矮，改 `0.5f` 这个比值即可。
